sudo: required
service:
  - docker
before_install:
  - docker build -t palaniec:dockerrepo:latest .
script:
  - echo "$docker_password" | docker lo gin -u "$docker_username" --password-stdin
  - docker images
  - docker push palaniec:dockerrepo:latest
notification:
  email:
    recipients:
      - kpalani.ec@gmail.com
    on_success: always
    on_failure: always
